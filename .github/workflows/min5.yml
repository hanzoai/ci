name: min5
on:
  workflow_call:
    inputs:
      runner:
        type: string
        default: '["zap-proto-build-linux-amd64"]'
jobs:
  hello:
    runs-on: zap-proto-build-linux-amd64
    steps:
      - run: echo inputs-only
